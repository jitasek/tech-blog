const router = require("express").Router();
const { User, Post, Comment } = require("../../models");

// Get all posts

// Get one post

// Create post

// Update post

// Delete post

module.exports = router;
