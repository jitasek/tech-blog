const router = require("express").Router();
const { User, Post, Comment } = require("../../models");

// Get all users
// router.get("/", (req, res) => {
//   User.findAll({});
// });

// Get one user

// Create user

module.exports = router;
