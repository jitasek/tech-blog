const router = require("express").Router();
const { User, Post, Comment } = require("../../models");

// Get all comments

// Get one comment

// Create comment

// Update comment

// Delete comment

module.exports = router;
